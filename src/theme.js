import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#3A5576',
		},
		secondary: {
			main: 'rgb(255,95,97)',
		},
		tierce: {
			main: '#427ABE',
		},
	},
});

export default theme;
